{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && cp -r ../playground/dist dist && cp -r ../docs/dist/docs dist"
  },
  "dependencies": {
    "docs": "workspace:*",
    "playground": "workspace:*"
  }
}
